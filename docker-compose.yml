services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-template
    expose:
      - "8000"
    environment: []
    networks:
      - network
    restart: unless-stopped

#  mariadb:
#    image: mysql:8.0
#    container_name: mariadb
#    environment:
#      - MYSQL_ROOT_PASSWORD=password
#      - MYSQL_DATABASE=db
#      - MYSQL_USER=user
#      - MYSQL_PASSWORD=password
#    volumes:
#      - mysql_data:/var/lib/mysql
#    networks:
#      - network
#    restart: unless-stopped
#    command:
#      - --max-connections=200
#      - --wait-timeout=600
#      - --interactive-timeout=600
#      - --max-allowed-packet=64M

#  phpmyadmin:
#    image: phpmyadmin/phpmyadmin
#    container_name: phpmyadmin
#    environment:
#      - PMA_USER=root
#      - PMA_PASSWORD=Fmg7MTQ+Qy7cVS0XuftNxXOY59GJ8Hh3jmoyMOtxkpg=
#    depends_on:
#      - mariadb
#    networks:
#      - network
#    restart: unless-stopped

networks:
  network:
    driver: bridge

# volumes:
#  mysql_data: